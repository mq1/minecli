on:
  push:
    tags:
    - '*'

jobs:
  build-linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Load Release URL File from release job
        uses: actions/download-artifact@v1
        with:
          name: release_url
      - name: Set up Python
        uses: actions/setup-python@v2
      - run: |
          pip install --user poetry
          python -m poetry install
          python -m poetry run python -OO -m PyInstaller --onefile minecli/minecli.py
          mv dist/minecli dist/minecli-linux
      - uses: ncipollo/release-action@v1
        with:
          allowUpdates: true
          prerelease: true
          artifact: ./dist/minecli-linux
          token: ${{ secrets.GITHUB_TOKEN }}

  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v2
      - name: Load Release URL File from release job
        uses: actions/download-artifact@v1
        with:
          name: release_url
      - name: Set up Python
        uses: actions/setup-python@v2
      - run: |
          pip install --user poetry
          python -m poetry install
          python -m poetry run python -OO -m PyInstaller --onefile minecli/minecli.py
      - uses: ncipollo/release-action@v1
        with:
          allowUpdates: true
          prerelease: true
          artifact: ./dist/minecli.exe
          token: ${{ secrets.GITHUB_TOKEN }}

  build-macos:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v2
      - name: Load Release URL File from release job
        uses: actions/download-artifact@v1
        with:
          name: release_url
      - name: Set up Python
        uses: actions/setup-python@v2
      - run: |
          pip install --user poetry
          python -m poetry install
          python -m poetry run python -OO -m PyInstaller --onefile minecli/minecli.py
          mv dist/minecli dist/minecli-macos
      - uses: ncipollo/release-action@v1
        with:
          allowUpdates: true
          prerelease: true
          artifact: ./dist/minecli-macos
          token: ${{ secrets.GITHUB_TOKEN }}
